# 部署检查清单

- 仓库根目录直接存在 `index.html`。
- `assets/svg/cover.svg` 可以在 GitHub 仓库中直接打开。
- `assets/images/label-glyph-1.png` 可以在 GitHub 仓库中直接打开。
- `css/share-page.css` 与 `css/result-page.css` 已上传。
- `js/quiz-results.js` 与 `js/ambient-music.js` 已上传。
- GitHub Pages 使用 `main` 分支和 `/ (root)`。
- 若出现破图，优先检查 `assets` 文件夹是否完整，而不是重复上传 HTML。
